import 'package:flutter/material.dart';

// TODO:
class PoemDiscoverView extends StatelessWidget {
  const PoemDiscoverView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child:
          Text("Bu sayfa henüz hazır değil\n\n\nThis page is not yet ready"),
    );
  }
}

import 'package:flutter/material.dart';

class DiscoverPoemSegmentView extends StatelessWidget {
  const DiscoverPoemSegmentView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child:
          Text("Bu sayfa henüz hazır değil\n\n\nThis page is not yet ready"),
    );
  }
}
